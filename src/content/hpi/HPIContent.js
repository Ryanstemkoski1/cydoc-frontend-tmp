import React, {Component, Fragment} from 'react';

//Component that manages the content for the HPI section.
export default class HPIContent extends Component{
    render() {
        return (
            <Fragment>
            {/*Christine, the components you have so far should be added here*/}
            </Fragment>);
    }
};